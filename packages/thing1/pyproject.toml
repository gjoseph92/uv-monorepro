[project]
name = "thing1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cowsay>=6.1",
]

[dependency-groups]
dev = [
    "py-spy>=0.4.0",
]
